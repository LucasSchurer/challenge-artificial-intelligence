meta {
  name: Create Duplicated User
  type: http
  seq: 2
}

post {
  url: {{host}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "username": "test_user",
    "password": "test_password",
    "name": "Test User"
  }
}

tests {
  test("Testing duplicated user", function () {
    expect(res.status).to.eql(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
