meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{host}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "username": "test_user",
    "password": "test_password",
    "name": "Test User"
  }
}

script:post-response {
  bru.setVar("_user_user_id", res.body.id)
}

settings {
  encodeUrl: true
  timeout: 0
}
