meta {
  name: Delete User
  type: http
  seq: 6
}

delete {
  url: {{host}}/user/{{_user_user_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
